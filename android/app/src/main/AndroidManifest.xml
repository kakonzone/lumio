<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- API 21+; security-crypto needs 23 at runtime — we skip it below 23 in MainActivity -->
    <uses-sdk
        android:minSdkVersion="21"
        tools:overrideLibrary="androidx.security.crypto" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <!-- Mediation / network classification (LevelPlay adapters). -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <!--
    Advertising ID (Android 13+): declared for mediation. Users who opt out in system
    Settings → Privacy → Ads see a zeroed/limited ID; mediation SDKs must respect limitAdTracking.
    Lumio does not request AD_ID at runtime beyond what LevelPlay pulls via GMS.
  -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <!-- API 33+ only; ignored on Android 5–12 at install time -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <!-- url_launcher: Adsterra direct link in external browser (Android 11+) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <package android:name="com.android.chrome" />
        <package android:name="com.mi.globalbrowser" />
        <package android:name="com.android.browser" />
        <package android:name="com.brave.browser" />
        <!-- Blocked-app detection (Android 11+ package visibility) -->
        <package android:name="com.guoshi.httpcanary" />
        <package android:name="com.guoshi.httpcanary.premium" />
        <package android:name="com.httpcanary.pro" />
        <package android:name="app.greyshirts.sslcapture" />
        <package android:name="jp.co.taosoftware.android.packetcapture" />
        <package android:name="com.minhui.networkcapture" />
        <package android:name="com.evbadroid.proxymon" />
        <package android:name="com.emanuelef.remote_capture" />
        <package android:name="de.robv.android.xposed.installer" />
        <package android:name="org.lsposed.manager" />
        <package android:name="com.topjohnwu.magisk" />
        <package android:name="io.github.vvb2060.magisk" />
        <package android:name="com.proxyman.NSProxy" />
        <package android:name="tech.httptoolkit.android.v1" />
        <package android:name="com.lbe.parallel.intl" />
        <package android:name="com.excelliance.dualaid" />
        <package android:name="com.ludashi.dualspace" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" android:host="www.effectivecpmnetwork.com" />
        </intent>
    </queries>

   <application
    android:label="Lumio"
    android:name="${applicationName}"
    android:icon="@mipmap/ic_launcher"
    android:allowBackup="false"
    android:fullBackupContent="false"
    android:usesCleartextTraffic="false"
    android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- World Cup campaign deep links (Facebook / WhatsApp / Telegram). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="lumio" android:host="open"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="lumio" android:host="channel"/>
            </intent-filter>
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="lumio.app"
                    android:pathPrefix="/open"/>
            </intent-filter>
        </activity>

        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Must match Dart LevelPlay.init app key (LEVELPLAY_APP_KEY dart-define). -->
        <meta-data
            android:name="com.ironsource.sdk.ApplicationKey"
            android:value="@string/levelplay_app_key" />

        <!-- Monetag push (WebView registration — separate from FCM match alerts). -->
        <meta-data
            android:name="lumio.monetag.push.channel"
            android:value="lumio_monetag_push" />
    </application>
</manifest>
