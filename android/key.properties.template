# Copy to android/key.properties (gitignored). Never commit key.properties or *.jks.
#
# Local file (recommended for dev machines):
storeFile=upload-keystore.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=upload
keyPassword=YOUR_KEY_PASSWORD
#
# storeFile path is relative to the android/ directory, or use an absolute path.
# Place upload-keystore.jks in android/ (also gitignored via **/*.jks).
